{
  "name": "swat-react-tooltip",
  "version": "0.0.1",
  "description": "A simple and lightweight tooltip for React",
  "main": "lib/tooltip.js",
  "scripts": {
    "compile": "rm -Rf lib && mkdir lib && babel --presets es2015,react -d lib/ src/",
    "prepublish": "npm run compile"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/weareswat/swat-react-tooltip.git"
  },
  "keywords": [
    "react",
    "tooltip",
    "swat"
  ],
  "author": "SWAT",
  "license": "ISC",
  "dependencies": {
    "react": "^15.2.1"
  },
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.11.1"
  }
}
